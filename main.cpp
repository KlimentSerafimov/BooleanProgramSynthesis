#include <iostream>

class Bitvector
{private:
    int bitvector;
public:
    Bitvector(int _bitvector)
    {
        bitvector = _bitvector;
    }
    //dude it's fine since you haven't done this sober!!!

};

int main() {

    //parameters:
    //n - the number of input variables
    //L - the language, given as a subset of {{0, 1}^2 -> {0, 1}}
    //train_set - set of input output examples, given as a subset of {({0, 1}^n, {0, 1})}
    //goal: synthesize a program in L

    for(int i = 0; i < 16; i++)
    {

    }

    return 0;
}
